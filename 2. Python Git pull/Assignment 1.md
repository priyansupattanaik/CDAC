
# 100 Python Interview / Viva Questions

# 1. Introduction to Python 

1. What is Python?
2. Who developed Python and when was it released?
3. Why is Python called an interpreted language?
4. What are the main features of Python?
5. What are the advantages of Python over other programming languages?
6. What are the major applications of Python?
7. What is the difference between Python 2 and Python 3?
8. What is PEP 8?
9. What is the role of pip in Python?
10. What is the difference between REPL mode and script mode?

# 2. Basic Syntax & Structure 

11. What is indentation in Python?
12. Why is indentation mandatory in Python?
13. What happens if indentation is incorrect?
14. What are Python keywords?
15. How many keywords are there in Python?
16. What is the difference between comments and docstrings?
17. How do you write single-line comments?
18. How do you write multi-line comments?
19. What is the use of the `print()` function?
20. What is the use of the `input()` function?

# 3. Variables & Data Types 

21. What is a variable in Python?
22. What are the rules for naming variables?
23. What is dynamic typing in Python?
24. What are built-in data types in Python?
25. What is the difference between `int` and `float`?
26. What is a complex number in Python?
27. What is the `bool` data type?
28. What does the `None` data type represent?
29. How do you check the type of a variable?
30. What is type casting?
31. How do you convert a string to an integer?
32. What is the difference between mutable and immutable data types?
33. Which Python data types are immutable?
34. Which Python data types are mutable?
35. What is multiple variable assignment?

Example:

```python
a, b, c = 10, 20, 30
```

# 4. Operators 

36. What are operators in Python?
37. What are arithmetic operators?
38. What is the difference between `/` and `//`?
39. What does the modulus operator `%` do?
40. What are comparison operators?
41. What are logical operators in Python?
42. What is the difference between `and` and `or`?
43. What is the `not` operator?
44. What are bitwise operators?
45. What is the difference between `&` and `and`?
46. What are assignment operators?
47. What are membership operators?
48. What is the `in` operator?
49. What are identity operators?
50. What is the difference between `is` and `==`?

# 5. Strings 

51. What is a string in Python?
52. How can you create a string in Python?
53. What is string indexing?
54. What is string slicing?
55. What is negative indexing?
56. Are Python strings mutable or immutable?
57. What are common string methods?
58. What does the `strip()` function do?
59. What is the difference between `split()` and `join()`?
60. What is string formatting?
61. What are f-strings?
62. What is the `.format()` method?
63. What are escape characters?
64. What does `\n` represent?
65. What does `\t` represent?

---

# 6. Control Flow Statements (66–80)

66. What are control flow statements?
67. What is the syntax of the `if` statement?
68. What is the difference between `if` and `elif`?
69. What is nested `if`?
70. What is the use of `else`?
71. What is the `match-case` statement?
72. In which Python version was `match-case` introduced?
73. What is the purpose of the `pass` statement?
74. What is the difference between `break` and `continue`?
75. What happens when `break` is used inside a loop?
76. What happens when `continue` is used inside a loop?
77. What is short-circuit evaluation?
78. How can you write multiple conditions in an `if` statement?
79. What is the ternary operator in Python?
80. How do you check whether a number is positive or negative?

Example:

```python
result = "Positive" if x > 0 else "Negative"
```

# 7. Loops 

81. What is a loop in programming?
82. What is the difference between `for` loop and `while` loop?
83. How does a `for` loop work in Python?
84. How does a `while` loop work?
85. What is an infinite loop?
86. What is the `range()` function?
87. What are the parameters of `range()`?
88. What is a nested loop?
89. What is loop control?
90. What does the `break` statement do?
91. What does the `continue` statement do?
92. What does the `else` block in loops do?
93. How do you iterate over a string using a loop?
94. How do you iterate over a list using a loop?
95. How do you reverse a loop using `range()`?

Example:

```python
for i in range(10, 0, -1):
    print(i)
```

---

96. What will be the output?

```python
print(True + True)
```

97. What will be the output?

```python
a = [1,2,3]
b = a
b.append(4)
print(a)
```

98. What will be the output?

```python
print(bool(""))
```

99. What will be the output?

```python
print(type(None))
```

100. What will be the output?

```python
print(5 == 5.0)
```





