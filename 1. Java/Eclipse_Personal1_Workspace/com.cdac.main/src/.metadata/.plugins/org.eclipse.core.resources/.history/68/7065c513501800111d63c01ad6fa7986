package com.cdac.main;

import java.util.stream.IntStream;

public class Program {

	public static void main(String[] args) {
		IntStream is=IntStream.range(1, 100);
		
	//	is.forEach(e->System.out.println(e));
		
		
	//	is.filter(n->n%2==0).limit(20).forEach(e->System.out.println(e));
	
	//	is.filter(n->n%2==0).filter(n->n>10).limit(3).forEach(e->System.out.println(e));
	
		int Sum=is.reduce(0, (a,b)->a+b);
		
		System.out.println(Sum);
		
		
		System.out.println("Max: "+is.max());
	}

}
